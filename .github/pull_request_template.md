## Summary

<!-- What does this PR do? Keep it to 1-3 sentences. -->

## Changes

<!-- Bullet list of what changed -->

## Testing

- [ ] `xcodebuild test` passes (134+ tests, 0 failures)
- [ ] `xcodebuild build -configuration Release` succeeds
- [ ] Tested manually in the app (if UI changes)

## Checklist

- [ ] No new external dependencies
- [ ] No force unwraps added
- [ ] Thread-safe (serial queue or @MainActor)
- [ ] Backward-compatible UserDefaults (Codable with defaults)
